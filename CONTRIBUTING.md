# Contributing to PromptPick

感谢你愿意帮助改进 PromptPick。

## 提交 Issue

请先搜索是否已有相同问题，并尽量提供：

- macOS 与 PromptPick 版本；
- 可重复的操作步骤；
- 预期结果与实际结果；
- 已删除敏感内容的截图或日志。

不要提交密码、Token、公司机密或真实剪贴板数据。

## 提交代码

1. Fork 仓库并从 `main` 创建功能分支；
2. 保持改动聚焦，不混入无关格式化；
3. 为行为变化补充单元测试或 UI 测试；
4. 在 PR 中说明根因、用户影响和验证方式；
5. 确认没有加入构建产物、个人 Xcode 配置和真实用户数据。

涉及窗口、Sheet、菜单或生命周期的修改，必须验证完整任务闭环：

`入口可发现 → 目标界面置前 → 关键动作可点击 → 可取消或完成 → 返回正确父页面 → 应用保持预期状态`

## 开发约束

- 支持 macOS 14 及以上；
- 优先使用系统原生 SwiftUI / AppKit 交互；
- 不增加未经说明的联网、遥测或剪贴板上传行为；
- 新依赖必须说明必要性、许可证和隐私影响；
- UI 测试运行前退出其他 `com.promptclip.app` 实例。

提交贡献即表示你同意将贡献按仓库适用的 MIT License 发布。
